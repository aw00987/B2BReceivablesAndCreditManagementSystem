<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aw00987.rcms.repository.UserMapper">

    <insert id="insert" parameterType="com.aw00987.rcms.entity.User"
            useGeneratedKeys="true" keyProperty="id" keyColumn="id">
        insert into users (
            username,
            password_hash,
            real_name,
            role,
            status
        ) values (
            #{username},
            #{passwordHash},
            #{realName},
            #{role},
            #{status}
        )
    </insert>

    <update id="update" parameterType="com.aw00987.rcms.entity.User">
        update users
        <set>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="passwordHash != null">
                password_hash = #{passwordHash},
            </if>
            <if test="realName != null">
                real_name = #{realName},
            </if>
            <if test="role != null">
                role = #{role},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>
